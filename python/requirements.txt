# ForgePipeline Python Inference Server
# Install: pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu124

# PyTorch (CUDA 12.4) - pip resolves compatible torch/torchvision pair
torch>=2.6.0
torchvision>=0.21.0

# AI model loading
diffusers>=0.33.0
transformers>=4.49.0
accelerate>=1.4.0

# Inference server
fastapi>=0.115.0
uvicorn>=0.34.0
python-multipart>=0.0.9

# Mesh processing
trimesh>=4.6.0

# FBX conversion (GLB -> FBX for Unreal Engine)
pyassimp>=5.2.5

# Material extraction (GLB texture parsing)
pygltflib>=1.16

# Image handling
Pillow>=11.0.0

# Background removal (required by Hunyuan3D)
rembg>=2.0.50

# Tensor operations (required by Hunyuan3D)
einops>=0.7.0

# Shap-E (lightweight image-to-3D mesh generation)
shap-e @ git+https://github.com/openai/shap-e.git

# Model downloading
huggingface-hub>=0.28.0

# VRAM monitoring
pynvml>=12.0.0

# Configuration
pyyaml>=6.0
